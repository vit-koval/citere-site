{%- if breadcrumbTrail | length > 1 %}
<nav class="crumbs" aria-label="Breadcrumb">{% for crumb in breadcrumbTrail %}{% if loop.last %}<span aria-current="page">{{ crumb.title }}</span>{% else %}<a href="{{ crumb.url | url }}">{{ crumb.title }}</a> / {% endif %}{% endfor %}</nav>
{%- endif %}
