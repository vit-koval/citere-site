// The Ukrainian mirror. CLAUDE.md 2: same structure, and a missing UA
// translation must fall back to English with the correct lang and a notice
// rather than failing the build.
module.exports = { ukEnabled: true, languages: ["en", "uk"] };
